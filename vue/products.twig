<!doctype html>
<html>
    <head>
        <title>{{ titre_doc }}</title>
        <meta charset='UTF-8'>
         <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
    </head>
     <header>
        <div class="inline">
            <img class="logo" src="productimages/livetoeat.jpg">
            <h1>CC & KAM Snacks</h1>
    </header>
    <body>
        {% if products is not empty %}
                <tbody>
                    {% for item in products %}

                        <div class="introduction">
                            <div class="d-flex align-items-center container gap-5 section ">
                                <div class="card" style="width: 18rem;">
                                    <img src={{lien[item.id]}}>
                                    <div class="card-body">
                                        <a href="controleur.php?action=detail&id={{item.id}}"><h5 class="card-title">{{ item.name }}</h5></a>
                                        <p class="card-text">{{ item.description|capitalize }}</p>
                                        <a href="controleur.php?action=addToCart&id={{item.id}}" ><img src="productimages/btnAddToCart.png" alt="image" /></a>
                                    </div>
                                </div>
                            </div>
                        </div>

                    {% endfor %}
                </tbody>                
        {% else %}
            Aucun produits à vendre.
        {% endif %}
    </body>
</html>

<style>
header {
    background-color:pink;
    
    padding: 15px 0;
}

.inline{
    display: inline-block;
    width:2000px;
    
}

.introduction {
    background-image: url('productimages/cute.jpg');
    height: 100vh;
    position: relative;
    overflow: hidden;
    background-color: #fff; 
    color: #333; 
}

.logo{
    width:100px;
    height:100px;
}

h1 { color: #F90B6D; font-family: "Great Vibes", cursive ; font-size: 40px; font-weight: 400; line-height: 40px; margin: 0 0 16px; }
h2 { color: #d54d7b; font-family: "Great Vibes", cursive; font-size: 35px; line-height: 40px; font-weight: normal; margin-bottom: 0px; margin-top: 40px; text-align: justify; text-shadow: 0 1px 1px #fff; }
p { color: #333333; font-size: 29px; font-family: "Libre Baskerville", serif; line-height: 45px; text-align: center; text-shadow: 0 1px 1px #fff; padding-top: 20px; }
a {  color: white; text-decoration: none; }

a {  color: purple; text-decoration: none; }

</style>